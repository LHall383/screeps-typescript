export enum RoleName {
    Harvester = "harvester",
    Builder = "builder",
    Upgrader = "upgrader",
    Transporter = "transporter",
    ContainerMiner = "containerMiner",
    Repairer = "repairer"
}
